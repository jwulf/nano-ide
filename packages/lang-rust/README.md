# @nanobpm/nano-ide-lang-rust

Rust language pack for the Nano RAD console IDE. Adds `.rs` editing (Monaco loads the Rust
grammar only when a Rust file is open), a `cargo` toolchain (run/compile via the user's
installed cargo), and a starter project template. Manifest: `nano-ide.ext.json`.
