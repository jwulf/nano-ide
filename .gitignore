node_modules/
dist/
*.tsbuildinfo
.DS_Store
# Rust build artifacts from example/template smoke builds
target/
Cargo.lock
# C# / .NET build artifacts from example/template smoke builds
bin/
obj/
# Python (uv) artifacts from example/template smoke builds
.venv/
__pycache__/
*.egg-info/
uv.lock

# Keep the rust-throughput template lock committed so the IntelliSense
# generator (cargo rustdoc --locked) is deterministic in CI.
!packages/lang-rust/templates/rust-throughput/Cargo.lock
